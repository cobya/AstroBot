//------------------------------------------------
// Name: bot_utils.js
// Author: Coby Allred
// Description: Contains various utility functions used across the bot's different features
//------------------------------------------------

module.exports = {
	// Do a channel based chat alert after a new subsciption
	subAlert(channel, username) {
		// Query db to see what sub alert message settings they have
	},

	// Do a channel based chat alert after a resub
	resubAlert(channel, username) {
		// Query db to see what sub alert message settings they have
	}
};